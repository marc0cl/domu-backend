# Domu Backend Application Configuration

# Server Configuration
server.port=7000

# Database Configuration
db.host=${DB_HOST}
db.port=${DB_PORT}
db.name=${DB_NAME}
db.user=${DB_USER}
db.password=${DB_PASSWORD}
db.uri=${DB_URI}
box.developerToken=${BOX_TOKEN}

mail.host=${MAIL_HOST}
mail.port=${MAIL_PORT}
mail.user=${MAIL_USER}
mail.password=${MAIL_PASSWORD}
mail.from=${MAIL_FROM}
approval.baseUrl=${APPROVAL_BASE_URL}
approval.recipient=${APPROVALS_RECIPIENT}

# Application Information
app.name=Domu Backend
app.version=1.0.0
app.description=Backend API for Domu project

# GCS Configuration
gcs.bucketName=${GCS_BUCKET_NAME}
gcs.keyFilePath=${GCS_KEY_FILE_PATH}
